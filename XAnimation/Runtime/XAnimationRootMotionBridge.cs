namespace XFramework.Animation
{
    // Intentionally left blank. The runtime bridge implementation lives in XAnimationActor.cs
    // so both Unity and generated csproj builds can resolve it consistently.
}
